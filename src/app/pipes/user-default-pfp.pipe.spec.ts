import { UserDefaultPfpPipe } from './user-default-pfp.pipe';

describe('UserDefaultPfpPipe', () => {
  it('create an instance', () => {
    const pipe = new UserDefaultPfpPipe();
    expect(pipe).toBeTruthy();
  });
});
