import { PostMediaPipe } from './post-media.pipe';

describe('PostMediaPipe', () => {
  it('create an instance', () => {
    const pipe = new PostMediaPipe();
    expect(pipe).toBeTruthy();
  });
});
