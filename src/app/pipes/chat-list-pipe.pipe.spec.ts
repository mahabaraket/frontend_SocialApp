import { ChatListPipePipe } from './chat-list-pipe.pipe';

describe('ChatListPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ChatListPipePipe();
    expect(pipe).toBeTruthy();
  });
});
