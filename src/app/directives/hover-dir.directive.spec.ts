import { HoverDirDirective } from './hover-dir.directive';

describe('HoverDirDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverDirDirective();
    expect(directive).toBeTruthy();
  });
});
